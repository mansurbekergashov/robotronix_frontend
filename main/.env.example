# Main Frontend — copy this to .env and adjust if needed
# Vite reads this file automatically during `npm run dev`

# URL of the running backend (Vite dev server proxies /api, /uploads, /ws here)
BACKEND_URL=http://localhost:8080
