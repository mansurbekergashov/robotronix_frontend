# Main Frontend — Vite proxy sozlamasi
# Bu faylni .env ga ko'chiring: cp .env.example .env

# --- Variant 1: Real server bilan ishlash (tavsiya) ---
# .env fayl kerak emas — vite.config.js o'zi https://robotronix.uz dan foydalanadi

# --- Variant 2: Lokal backend bilan ishlash ---
# Quyidagini .env ga yozing (lokal backend port 8080 da ishlab turishi kerak):
# BACKEND_URL=http://localhost:8080
